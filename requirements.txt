elevenlabs>=2.0.0
sounddevice>=0.5.0
numpy>=2.0.0
pynput>=1.7.7
# macOS only, and already arriving through pynput — declared because this project imports
# them itself: AppKit for handing the focus back, HIServices for the Accessibility check.
pyobjc-framework-Cocoa>=8.0; sys_platform == "darwin"
pyobjc-framework-ApplicationServices>=8.0; sys_platform == "darwin"
# The rewrite mode only. Imported lazily, so a missing wheel costs nothing until the
# mode is switched on — and even then the raw transcript is what lands.
google-genai>=2.0.0
pyperclip>=1.9.0
pystray>=0.19.5
# pystray accepts any Pillow. Majors ship every October and have broken it before, so the
# ceiling is deliberate — raise it once Pillow 13 has been tried against the tray icon.
pillow>=10.0.0,<13
python-dotenv>=1.0.0
